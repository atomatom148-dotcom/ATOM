# V-1A Amendment 3C - V1B Operational Activation

Decision ID: ATOM-V1A-AMENDMENT-3C-V1B-OPERATIONAL-ACTIVATION-1
Status: PROPOSED. No effect before §12's independent exact-final-head review, green required checks, zero unresolved
material findings, and Owner merge.
Author: ChatGPT Pro - architecture and freeze authority
Date: 2026-09-09
Repository: atomatom148-dotcom/ATOM
Sole documentation-PR path: docs/v-1a-amendment-3c-v1b-operational-activation.md, mode 100644
Operational checklist executor after effectivity: Codex, within the existing authorized control paths and the action-time
gates below.
Merge, credential, infrastructure, disruption, expenditure and invocation authority: Owner.

## 1. Decision and retained law
Authorize only the remaining, ordered operational preparation for one evidence-capable native Render one-off V1B
invocation, for one exact frozen manifest, using the implementation integrated by PR #333. The separately gated,
secret-free provenance probe is preparation, not an evidence-capable invocation or research look. Exact same-seal recovery
remains available only under the retained recovery contract and a separate action-time Owner approval for each submission.
This amendment does not authorize another implementation integration, continuous worker, scheduler, cron, Render
Workflow workload, readiness-polling loop, automatic retry, parallel invocation, broker operation, capital use, model promotion
or production-consumer change. It creates no new service, runtime credential name, credential source, queue, writer,
ingestion pause, replay mechanism, migration ordinal, scientific input or runtime/receipt schema. It does not authorize
restarting or rotating anything merely because this document has merged.
The controlling law remains:
- docs/v-1a-volatility-first-freeze.md;
- docs/v-1a-amendment-1-tls-trust-anchor.md;
- docs/v-1a-amendment-2a-tiered-readiness-boundaries.md;
- docs/v-1a-amendment-3-v1b-operational-prerequisites.md;
- docs/v-1a-amendment-3a-bootstrap-deadline-closure.md, as prospectively adopted and corrected by
Amendment 3B;
- docs/v-1a-amendment-3b-v1b-corrective-authorization.md;
- FREEZE.md, AGENTS.md and the effective role-governance freeze, subject only to their already effective exceptions.
This document supplies an action-gated operational handoff after the lawful implementation merge. Its read-only inspection
authorization is the narrow pre-activation inspection described in §4; it is not permission to invoke the scorecard or use a
runtime credential early. The exact commands, administrative SQL, canonicalization, deadlines, source binding and failure
routes in the incorporated law remain controlling. References to Amendment 3 always mean its text as corrected by
Amendments 3A and 3B, never a superseded capacity, native-attestation or restart-duration requirement.
Preserve all V1A/TLS/2A mathematics and evidence rules: the original T_amend, targets, horizons, manifest/cell partition,
earliest qualifying boundaries, readiness minima including the 1H necessary floor, causal scale/lineage/cohort rules,
benchmarks, numerical order, bootstrap/RNG streams, thresholds, global multiplicity and one-look accounting. Keep the eight
Amendment 2A evidence files, root documents and active SIM-5 pointer unchanged. Neither infrastructure observations nor
an Owner approval can make an insufficient population ready.

## 2. Bound implementation integration
The following are fixed identities, not interchangeable candidates:

| Identity | Required value |
|---|---|
| Implementation PR | atomatom148-dotcom/ATOM#333 |
| Independently reviewed implementation head | 7d313a6b8371d1b84b1e0e2bcd72085ccb6241bf |
| Implementation integration, M_impl | 73d1f21403144f62a0abf12c3ea3a163e2cd6cd2 |
| Amendment 3B merge, M_3B | 8e08a58f696459970ec8f991a37e2c914a5bc911 |
| Repository ID | 1339927428 |
| Owner identity | login atomatom148-dotcom, numeric ID 307819087 |

The authenticated main read used to author this proposal returned M_impl. That is an inspection baseline, not the eventual
execution revision and not a full action-time history audit. PR #333's authenticated metadata reports the stated reviewed head
and merge; its changed-file listing contains the following five paths. Operational acceptance must independently verify the
underlying review, checks, merge authority, history and bytes rather than accept a PR description as proof.
V1B_CHANGE_PATHS remains exactly:

```text
quant/volatility_scorecard.py
tests/test_volatility_scorecard.py
requirements.txt
certs/supabase-prod-ca-2021.crt
migrations/033_authorize_v1_volatility_scorecard_reader.sql
```

At Gate 1 and again at every retained execution checkpoint, authenticate the repository/default branch, complete PR
metadata, signed merge/parent identities, first-parent ancestry and complete integration history required by Amendment 3B
§3. In particular, M_impl must have first parent M_3B and second parent the stated reviewed implementation head. Verify the
authorized five-path diff and the exact mode, presence, Git blob and raw-byte hash binding between that head, M_impl and
the execution worktree. Bind and verify the complete unchanged reused-source set from the implementation review and
merged verifier; do not substitute a guessed or abbreviated set.
Audit from V1A adoption ac85cc9e99ccc499789f3ef79b186768d99fb0d6 through the actual execution revision. There
must be exactly one V1B implementation integration: M_impl. Reject an additional integration touching any of the five paths
or otherwise implementing V1B, including a direct push, renamed/hidden implementation, squash/rebase integration, repair,
reverted change or changed-and-restored interval. Retain the separate unchanged-reused-source history rule. Endpoint
equality alone is insufficient.
Let M_3C be this document's actual authenticated lawful Owner merge, recorded only after it exists. Select the exact full
40-lowercase-hex execution revision E from authenticated current main after that merge, with:

```text
M_3B < M_impl < M_3C <= E
```

Here the relation is the retained ordered first-parent relation. Disjoint, separately authorized changes outside an active
no-ref-update window remain subject to Amendment 3B §3; they cannot alter a bound source. No main movement is allowed
inside the window. Record this document's actual PR, final reviewed head, merge, mode, blob and raw SHA-256 in the
existing private acceptance packet. Do not invent them now, embed a self-digest, change the verifier to add them, or open a
second implementation PR. Its lawful adoption is an external operational gate, bound into the existing control-plane evidence
record.

## 3. Gate semantics and action accounting
The only ordinary forward sequence is:

```text
lawful 3C merge
-> read-only repository/infrastructure inspection
-> Owner-approved secret-free build/probe and independent acceptance
-> independently accepted exclusive no-ref-update window
-> Owner-approved reader fence and fixed termination passes
-> separately Owner-approved single password rotation
-> separately Owner-approved single project restart
-> actual continuity reconciliation and scoped pooler disposition
[one separately Owner-approved provider purge only if necessary]
-> separately Owner-approved two-secret installation
-> separately Owner-approved guarded LOGIN restoration/finalization
-> separately Owner-approved conditional migration 033, if needed
-> independently reviewed payload v2 and Owner operational approval
-> action-time Owner approval for one exact native invocation
-> retained terminal/recovery, publication and closure procedure
```

Documentation merge authorizes inspection and preparation of evidence/checklists, not the mutating actions listed above.
Before each action-time gate, Codex presents the exact target, proposed operation, applicable retained procedure,
independently reviewed evidence, known failures/uncertainties and prior-action accounting, then stops. The Owner must
affirmatively approve that specific action on the actual current facts. A prior “go,” implementation merge, general activation
request or approval of a different gate is not that approval. An approval cannot waive a failed prerequisite. Evidence drift
invalidates the dependent approval. Pre-action review accepts a supported procedure and existing protections; it does not
certify the future outcome. Completion evidence becomes mandatory at its specified post-action gate, never an impossible
prerequisite to performing the action that produces it.
Record submissions, acknowledgements, actual completion evidence, unknown outcomes and rejection separately in the
existing private packet. These are operations records, not new scorecard statuses or receipts. All record digests used publicly
cover non-secret evidence, never credential bytes or verifiers.
The single rotation, single restart and conditional single reader-scoped provider operation are the existing incident budgets,
not fresh budgets granted by 3C. Inspect whether each has already occurred. Credit a prior action only when its lawful
authorization, exact identity, completion and continuing predicates are independently established. Preserve its original
timestamps and records; do not repeat it to obtain a newer receipt. An uncertain or unauthorized prior action requires the
retained incident disposition, not grandfathering. A valid pending seal prohibits a replacement build or new look; use only its
retained exact-recovery path.
Protective stopping and authorized credential revocation retain their existing emergency authority. They do not authorize a ref
update, new password, additional restart, fabricated receipt or replacement look.

## 4. Gate 1 - Read-only inspection and preparation
After lawful merge, Codex may use existing authorized authenticated read/control-plane connections to inspect the exact
repository and operational targets. No scorecard invocation, protected calculation, reader-credential test, new secret or
mutation is included in this inspection grant. Administrative operations in later gates still use their exact existing
connection/credential routes; this gate is not an alternate administrative launcher.
The operational database binding is:
```text
Supabase project = afyiydxbjgzaiswnbcyj
database         = postgres
direct host      = db.afyiydxbjgzaiswnbcyj.supabase.co
direct port      = 5432
reader           = atom_e1_scorecard_reader
Render service   = srv-daa7thgae00c73a2lmn0 (atom-h2d3-benchmark)
```

Retain current authenticated project/host/database identity; the reader's actual OID, name and attributes; memberships, role
settings, effective grants and RLS/catalog state; reader-owned prepared transactions; and permitted reader-backend
metadata without SQL text or credential-bearing fields. The OID is observed, not specified by this document, and must
thereafter survive the exact name/OID guards. Record the control-system identifier, server version, current postmaster
generation, and provider inventory of primary/replica/other serving instances. Unknown coverage is unknown, not zero. The
restart route requires one primary and zero replicas/secondary serving instances; this amendment authorizes no removal or
replacement to achieve that state.
Inspect effective Render key-name/configuration sources, including linked environment groups, secret files, blueprint/build
injection, selected build, commands, suspension and complete relevant job history. Verify URI absence where required; a
missing service override is not effective absence. Do not retrieve or print secret values merely to prove absence. Retain
current direct-host reachability and applicable existing setup evidence without weakening TLS, choosing a pooler or silently
buying/changing networking.
Inventory every applicable Shared Supavisor session and transaction plane, Dedicated Pooler and legacy pooler for this exact
project and reader. Obtain provider-scoped factual coverage, absence evidence where a plane does not apply, and any
supported procedure necessary for later disposition. Generic documentation is not a project-and-reader-scoped disposition.
Request no disclosure of a password, verifier, token or full URI to a support transcript.
Inspect the existing continuity protections for every input, writer, retry, lease and due obligation affected by the sole project's
interruption, not just V1B's read path. Retain existing source/destination identities, immutable prefixes, watermarks,
retention/acknowledgement behavior and exhaustion/backpressure contracts. No new queue, writer, source, pause, replay
mechanism or configuration change is authorized to manufacture a passing inventory.
Also inspect earlier incident records, approvals/windows, seals, receipts and submitted actions. Unknown prior job or action
state must be reconciled before the corresponding forward gate. This inspection may produce a blocker list; it does not
establish scientific readiness or claim that any later gate has passed.

## 5. Gates 2-3 - Secret-free artifact acceptance and exclusive window

### Gate 2: Owner-approved preparation, then independent probe review
The Owner must approve the exact service, E, unchanged 4c-8g capacity, existing build configuration and sole
non-consuming provenance probe. Require all earlier jobs terminal and no valid seal awaiting recovery. Reserve the existing
exclusive configuration/build-writer window. Keep automatic deploys/previews off. The exact inert base command is:

```text
python -I -S -B -X pycache_prefix=/dev/null/atom-v1b-no-pyc -c "raise SystemExit(0)"
```

Follow Amendment 3 §2.5 as corrected by 3A/3B. Both runtime secret keys and all frozen startup-injection keys must be
effectively absent. Use only its authorized service-scoped cleanup; a shared/baked/other unremovable injection source blocks
preparation rather than authorizing edits elsewhere. A necessary base resume is permitted only for that existing secret-free
preparation exception, with the inert command unchanged. Reverify suspension before the probe. Neither a continuous
scorecard command nor a secret-bearing base resume is permitted.
Build exactly E with pip install -r requirements.txt and PYTHON_VERSION=3.14.3. Use the actual successful
build-producing deploy ID B, with authenticated commit.id = E; do not mistake a deploy request or configuration-only
deployment for a successful build. Submit only the exact provenance_probe_main() one-off command from Amendment
3 §2.4 with Amendment 3A's exact hook transformation. Do not replace it with a shortened launcher or add arguments, a
manifest, database access, network research or a capacity probe. Resolve an uncertain submission by job listing/retrieval, not
another POST.
Retain the actual probe job ID, complete required stdout/exit/termination evidence, bracketing source/build/configuration
observations, exact successful component hashes and unchanged ATOM-V1B-RUNTIME-PROVENANCE-1 record P.
Independently review the measured artifact closure and startup observations against the reviewed source, pinned CA, exact
versions, approved dependency/native closure and all retained guards. A successful version print or self-reported digest alone
is not independent approval. No failed, missing or truncated probe output is a successful baseline.
Amendment 3B §5's managed-platform trust boundary controls: no cross-job native-system-layer attestation is required or
claimed. The private startup record must state that boundary and retain all observable controls; runtime checks do not prove
that a future pre-interpreter hook cannot see environment secrets. Preserve actual-invocation dispatch measurement and
exact sealed-runtime equality for recovery.
After successful probe termination and independent acceptance, the Owner may separately approve provisioning of the one
dedicated PAT under Amendment 3 §3 and retain its exact non-secret scope evidence. Do not install either runtime secret
yet. Existing permitted secure handling applies; no file, alternate secret store, replacement token or early installation is
introduced.

### Gate 3: Owner opening and independent acceptance of the exact window
With both runtime secrets still absent, the Owner opens the Amendment 3 §4 exclusive no-ref-update window for exactly E
and one manifest M. Select M only from the unchanged nine-manifest registry and retained eligibility rules; this is not authority
to run all nine. An unsatisfied early-terminal prerequisite cannot be bypassed by selecting a later manifest.
Require authenticated repository/default-branch identity, main = E, zero pending/uncertain ref writers and lawful disposition
of every earlier repository-wide V1B window. Create/read back only the retained temporary rule
ATOM-V1B-NO-MAIN-UPDATE-1, literal refs/heads/main, active update/deletion/non-fast-forward restrictions and no
bypass actors, with the exact canonical W and complete opening history/version evidence specified by Amendment 3 §4.
Independent review must accept the real opening record before the next gate.
Keep the exclusive ref/settings-writer gate and selected rule continuously effective. No merge or ref update of any
kind-including documentation, SIM, HIST8, rollback or receipt merges-is permitted until lawful closure. Retain the independent
build/configuration-writer exclusion; hold the exact reader-role/project-infrastructure writer exclusion through incident
finalization and the migration gate. A missing rule/history/bypass proof is a blocker, not something an Owner promise
replaces. If main moved before opening, the old build/revision evidence is not silently relabeled as current.

## 6. Gates 4-5 - Fence, one rotation and one lossless restart

### Gate 4A: Owner-approved fence and exact visible-reader termination
Use only Amendment 3 §6.3's existing direct administrative terminal/connection, pinned CA, private prompt and exact
identity/authorization preflight. Retain the real initial private incident
ATOM-SEC-INCIDENT-V1B-READER-CREDENTIAL-2026-09-06, with truthful PENDING/OPEN state where completion
has not occurred. Missing preflight privilege authorizes no grant, fallback or wider administrator.
After action-time Owner approval, perform only the exact guarded NOLOGIN transaction and its acknowledged completion
bound, followed by the two fixed OID-selected visible-reader termination transactions and their prescribed
separation/checkpoints. Retain every target (pid, backend_start), boolean, exception, acknowledgement and
reader/prepared/settings zero proof. Do not add a pass, select saved PIDs, mutate another role, clear prepared transactions
or reset settings. Visible zeros are not proof about unpublished startup children or pooler frontends.

Keep both runtime secrets absent, the base suspended/inert, zero V1B jobs, and the exclusive windows intact. If provider
evidence establishes that complete scoped disposition cannot be supplied or that a necessary conforming operation is
unavailable, stop the forward sequence: require verified NOLOGIN, URI absence, an OPEN incident and V1B blocked. A final
disposition that is still legitimately pending the approved rotation/restart is not an assertion of provider inability; it must actually
pass Gate 6 before restoration. An unverified role state must be reported as unverified containment, not described as fenced;
use only existing authorized protective operations to establish the fence.

### Gate 4B: Separate Owner approval for the single password rotation
Before approval, independently review the exact reader/project binding, known exposure/action history, fence/termination
evidence, available provider disposition path and the existing lossless restart-continuity plan. Do not rotate/restart merely to
experiment with an unsupported pooler remedy or a known unprotected input path.
After the separate approval, execute exactly Amendment 3 §6.3's one fresh password transaction: retained server-clock lower
bound, rotation_xid8, SCRAM setting, exact precondition, private \password action and row-version postcondition, with
no unrelated role mutation. Require acknowledged COMMIT and the exact immediately subsequent canonical server-clock
completion output. Preserve that value as an observed completion upper bound, never an invented internal commit instant.
Retain only the permitted non-secret row-version/status evidence, not the password, verifier or verifier digest.
An uncertain password COMMIT or missing required completion output leaves PENDING/OPEN: no project restart, extra
reconnect, repeat rotation, old-password test or exposed-password restoration is authorized. Follow the retained
documentation-first incident route. 3C does not cure that uncertainty by permitting another rotation.

### Gate 5: Independently reviewed containment and separate restart approval
Apply Amendment 3B §6 in place of all removed hard-duration contracts. Before the Owner confirms the one top-level Restart
project action on afyiydxbjgzaiswnbcyj, retain the complete independently reviewed inventory, actual server-clock
t_guard_start, immutable prefixes and watermarks, zero unresolved pre-guard commit ambiguity, and zero unprotected
accepted inputs, writes, retries, leases or due obligations.
Every affected path must already satisfy either the retained no-input/no-due-obligation condition for the actual interruption, or
existing external durable capture and reconciliation. A finite quiet interval is insufficient if required inputs may resume during
an unbounded restart. Required records must not expire, be overwritten, be destructively acknowledged or be rejected when
capture is required; existing exhaustion/backpressure behavior must preserve them until destination commit and identity/hash
reconciliation. Market closure, sampled health and an in-memory queue do not establish this protection. If existing behavior
cannot pass, do not restart.
Obtain affirmative action-time Owner disruption approval after that review and after rechecking one primary/zero replicas,
verified NOLOGIN, URI absence, acknowledged rotation, active windows and suspended/inert base with zero V1B jobs. Retain
original-generation-guarded t_submit using the existing server-clock query and submit the exact approved action once.
Keep the containment contracts effective through actual completion and reconciliation.
Retain actual project/action identity, acknowledgement and completion evidence, provider timestamps with their stated
meanings, and observed bounds when an internal timestamp is unavailable. An uncertain submission is reconciled through
the same provider control plane/support; never submit a second restart because acknowledgement is missing. A toast,
healthy state, dropped connection or elapsed wait is not completion proof.
Only the retained post-completion reconnect and canonical variable rehydration are permitted. Prove the same control-system
identity, the exact unchanged committed rotation row under the retained guards, a strictly newer primary postmaster
generation satisfying the original t_submit comparison, and current one-primary/zero-replica inventory. Do not manufacture
D_submit_max, D_restart_max, D_post_max, a deadline-derived safe end, a guessed SLA or a zero/infinite substitute.
Their removal does not relax the unchanged SQL safeguards or GitHub transport deadlines.

## 7. Gate 6 - Actual reconciliation and scoped provider disposition
Continuity acceptance
Retain observed t_reconciliation_cutoff and later t_recovery_complete bounds using the existing query forms.
For each inventoried path, prove the pre-action durable prefix unchanged and account for every required identity through the
cutoff. No-input paths must actually have had none. Each durable-path identity must map exactly once to an
identity/hash-verified destination commit or one still-retained durable pending record, with contiguous watermarks and no
unexplained gap, duplicate, truncation, changed event time or ambiguous destructive acknowledgement.
Pending accounted work is not activation readiness. Only the already authorized writer may complete it. Independently
reconcile a final fixed maintenance cohort ending at a newly observed post-restart cutoff, including every previously identified
pending identity; all of that cohort must be committed before activation. Subsequent traffic must remain protected by the same
existing contracts. This does not demand that all future queues remain permanently empty.
Missing, expired or irreconcilable evidence, incomplete restart coverage, unknown inventory or a noncommitted final cohort
leaves the incident OPEN, the reader verifiably NOLOGIN, the URI absent and V1B blocked. No backfill, imputation,
timestamp repair, evidence alteration or second restart is authorized to obtain a pass.

### Provider scope review and conditional purge gate
Require Amendment 3B §7's authenticated project-and-reader-scoped Supabase support/control-plane disposition
identifying project, database, exact reader name and retained OID. It must cover every applicable Shared Supavisor
session/transaction, Dedicated and legacy pooler plane, or supply authoritative evidence of a plane's absence. Distinguish
observed facts, actions and provider guarantees, including coverage limits and the completion event or applicable deployed
behavior.
Acceptance must establish that every pre-rotation authenticated reader frontend and queued/server lease was terminated or
irrevocably contained, and that no stale verifier or previously authenticated frontend can regain reader database access after
LOGIN restoration. Coverage must include the entire fenced period and the relevant restoration behavior, not just an earlier
snapshot. Individual internal session IDs are unnecessary when provider evidence supplies complete scoped coverage.
If provider evidence establishes that an operation is necessary, retain its exact supported procedure, covered planes,
reader/project scope, effects and required completion evidence. Obtain independent scope review and a separate
action-time Owner approval before the one coordinated provider-executed reader-scoped drain/purge. It may occur only
after acknowledged password rotation and completed primary restart, while verified NOLOGIN, URI absence, zero V1B jobs
and the exclusive role/project window remain in force. Capture actual completion and reconcile uncertainty without blind
repetition.
No effect on another role, grant, credential, database instance, source or writer; second rotation/restart; or project-wide
interruption affecting other clients is authorized by this purge gate. No guessed API, self-hosted administrative command,
permission expansion or secret-bearing support disclosure is permitted. Widening requires a separate amendment. If
complete disposition is established without an operation, do not perform a speculative purge.
If Supabase cannot provide the required complete scoped disposition, or cannot perform a necessary conforming
operation: verified NOLOGIN; URI absent; incident OPEN; V1B blocked. No Owner assertion, elapsed cache wait, failed
old-password test, generic FAQ, self-hosted source code, newer postmaster or repeated PostgreSQL zeros substitutes for
that evidence. Both continuity acceptance and provider disposition must pass before Gate 7.

## 8. Gate 7 - Exactly two secrets, guarded LOGIN restoration and finalization

### Installation approval
After accepted probe/build/window, rotation, restart, continuity and pooler evidence, the Owner must separately approve
installation of exactly:

```text
ATOM_E1_SCORECARD_READONLY_DATABASE_URL
ATOM_V1B_GITHUB_TOKEN
```

Use only the original sources and scope. The reader URL contains the one newly rotated password and preserves:

```text
host         = db.afyiydxbjgzaiswnbcyj.supabase.co
port         = 5432
database     = postgres
user         = atom_e1_scorecard_reader
sslmode      = verify-full
sslrootcert = certs/supabase-prod-ca-2021.crt
sslcertmode = disable
require_auth = scram-sha-256
gssencmode   = disable
```

The CA remains 1,367 bytes, SHA-256
700723581420dd1ac98fd7e9ac529f0ef210eadcaf87fc868a3ad7d114c2f3b7. Preserve all raw-source, duplicate,
decoding, dual-parser, PQconninfo, explicit-password and no-ambient/file/argument/pooler checks. A provisioning
correction may use only that same rotated credential under the existing rules; it is not another password rotation.
The PAT is fine-grained, resource owner atomatom148-dotcom, repository selection only atomatom148-dotcom/ATOM,
Contents:read plus implicit Metadata:read, all other optional permissions No access and no write scope. Retain correlatable
non-secret scope evidence for independent review. Successful API reads alone do not prove scope. The scorecard reads it
only from its exact injected variable, in memory; no alias, CLI login, helper, file, extra API permission or fallback is allowed.
Install no Render/Supabase administrative credential in V1B.
Install using the existing Save only route while NOLOGIN remains verified, the base is suspended/inert and no job exists.
Effective URI absence is required through the accepted pooler disposition and ends only at this guarded provisioning event.
Re-read configuration: relative to the accepted secret-free baseline only these two authorized secret keys may have been
added; all startup-injection keys remain absent, and B, E, commands and every other approved non-secret setting remain
exact. The already authorized non-secret ATOM_V1B_AUTHORIZED_MAIN_SHA equals E; it is not a third secret or
provenance carrier. No rebuild or secret-bearing base resume is permitted.
Never log/serialize passwords, full URIs, token bytes or digests, password verifiers/digests, authorization headers, full
environments or secret-bearing exceptions. Existing private secure handling and redaction rules remain unchanged.

### Restoration approval
Stop for separate Owner approval of the exact guarded LOGIN restoration. Only after every inherited restoration guard
passes, execute Amendment 3 §6.3's unchanged restoration transaction, acknowledgement/completion bound,
reader/prepared/settings zero observations and final checks. Preserve the immutable OID and all other frozen role attributes.
Do not weaken migration 033's LOGIN precondition or restore LOGIN merely to make migration possible.
Complete and independently accept the real private incident finalization under Amendment 3B §§6-7 and retained
Amendment 3 §6.1. Bind actual fence, rotation, restart, pooler, URI-provisioning and restoration facts separately. Rotation
completion remains its original confirmed upper-bound timestamp. Preserve initial/open versions and disclose investigation
limits; finalization is not a claim of historical non-abuse. No unavailable or guessed proof is marked completed.

## 9. Gate 8 - Conditional application of the exact merged migration

Migration 033 is included in M_impl; inclusion is not application. Its exact existing preconditions require the steady-state
reader to have LOGIN. Therefore this gate follows lawful Gate 7 restoration and incident finalization; it does not run during the
NOLOGIN fence. Keep the base suspended/inert, zero V1B jobs, unchanged B/E and the exclusive relevant writers through
this gate.
First inspect required reader access and authenticated application history. If a prior lawful application and exact final
privilege/RLS state are independently established, record that evidence and skip application; do not rerun a one-time
migration. If the required access is missing, independently review the exact starting state and stop for separate action-time
Owner approval of one transaction containing only:

```text
migrations/033_authorize_v1_volatility_scorecard_reader.sql
```

Use its exact authenticated bytes from M_impl, Git blob 542df8f2c84e492a271547623e94f518c1d7fbae.
Independently compute and retain their complete 64-lowercase-hex raw SHA-256 in the reviewed action record; require the
proposed and transmitted migration file bytes to match that authenticated blob and digest exactly. A copied PR-description
hash is not authoritative. No edits, extra grant, migration ordinal, role repair or schema relaxation is authorized.
Before transmitting any migration SQL byte, pass exactly one existing V1A §15.2 project-binding route: the Supabase MCP
get_project proof of exact id, ref and direct host followed by apply_migration for that same project, or its fully
checked direct administrative-DSN route with postgres as the sole login substitution. Preserve the applicable
credential-source and pinned-CA rules. No dashboard/SQL-editor paste, linked-CLI default, pooler or other transport is
allowed. Verify the selected method applies the file atomically as one transaction; do not assume that from its name. Keep
the file's local 500ms lock timeout and 15s statement timeout unchanged.
Repeat the inherited name/OID guard externally against the retained reader OID under the exclusive writer window. Require
every exact in-file starting assertion, not merely a missing-SELECT finding. These include SQL current_user =
postgres, current_database() = postgres, exact steady-state role attributes, zero memberships, unchanged
database ownership/CREATE/public-only TEMPORARY and schema authority, the exact four prior SELECT grants, two
expected postgres-owned RLS tables, absent target access/applicable reader policies and the exact six recognized existing
policies. A false precondition stops; do not mutate the starting state to manufacture a pass. The file must repeat its own
fail-before-mutation assertions within the application transaction.
Its only privilege mutation is SELECT on public.volatility_forecasts and
public.volatility_forecast_outcomes, plus the two exact reader-specific permissive SELECT policies. Require all
in-file final assertions and, after acknowledged COMMIT, independent read-only verification of the exact six-table effective
read/no-write state, zero non-system durable-relation writes/CREATE, retained role/database/schema attributes and
public-only TEMPORARY, the two authorized proof-reader EXECUTE permissions and no additional allowed non-system
SECURITY DEFINER routines, exact policy names/roles/predicates/counts, preserved existing policies and absence of
applicable restrictive SELECT policies. Use catalog/privilege inspection; do not compute protected results to test access.
A failed assertion/statement must abort the whole transaction. Retain its actual error stage, acknowledged rollback or other
known transaction outcome and observed catalog state. A lost response or ambiguous COMMIT is not proof of rollback or
success. Stop and reconcile through the permitted read-only/history/incident paths before any further action; no blind rerun,
partial application, compensating grant, policy deletion or improvised repair is authorized. No V1B invocation proceeds while
migration outcome or final privilege/RLS state is unresolved.

## 10. Gate 9 - Payload v2, final approval and the one invocation
### Complete operational approval
Only after all preceding applicable gates pass, construct the existing A using actual evidence. Preserve Amendment 3 §2.6's
exact keys, P, W, canonical hashing and independent-review/Owner-approval envelopes, with only Amendment 3B §4's
already merged replacements:

```text
A.schema_version = ATOM-V1B-OPERATIONAL-APPROVAL-PAYLOAD-2
```

```json
{"mechanism":"Render native one-off Create job startCommand","recovery_transport_policy":
"EXACT_SUBMISSION_OR_CONSUMING_INCIDENT","service_id":"srv-daa7thgae00c73a2lmn0"}
```

The JSON above is exactly A.capacity. Keep the other fields: provenance, provenance_sha256,
no_ref_update_window, no_ref_update_window_sha256, probe_job_id, probe_observation_sha256,
control_plane_evidence_sha256, pat_scope_evidence_sha256, incident_record_id,
incident_finalization_sha256, rotation_status and rotation_completion_timestamp. Populate the last
two as COMPLETED and the original actual timestamp only after verified completion. Recompute every enclosing hash from
actual canonical bytes. This text is not a populated approval payload.
The existing private packet and non-secret control-plane evidence binding must retain this amendment's lawful adoption and
every applicable gate record, including migration disposition. Do not add approval, readiness, run, seal or receipt
keys/versions. Do not reintroduce a vendor command-length maximum, reserve/capacity inequality or cross-job native-layer
attestation.
Independent operational review must inspect the actual underlying packet, measured artifact/startup evidence, credential
scope, final incident, current privilege/RLS state and complete window/closure history. A digest alone or synthetic test is not
proof. Then publish the exact retained unedited review and Owner envelopes, in that order, through the existing
Owner/reviewer GitHub connections in PR #325's discussion, not through the runtime PAT. The outer schemas remain
ATOM-V1B-OPERATIONAL-REVIEW-1 and ATOM-V1B-OPERATIONAL-APPROVAL-1; only the payload is version 2.
Preserve authenticated reviewer/Owner identities, independence, prior-review linkage and canonical hashes.
Retain and examine version-1 approvals under the historical-window/duplicate rules; do not delete or ignore them. An active
or ambiguously closed old approval blocks launch. Require exactly one current version-2 approval and zero overlapping
repository-wide windows. No latest-comment choice or unverified closure is permitted.

### Action-time invocation approval and runtime readiness
Codex stops again. The Owner's action-time approval must identify M_3C, PR #333/M_impl, exact E, accepted B, service,
exact manifest M, accepted window ID, payload digest and exact merged startCommand, and authorize one native Create-job
submission. It must follow current checks showing no prior accepted/uncertain submission for this attempt, no live V1B job, no
conflicting pending seal, exact selected artifact/environment and the active unchanged window. An approval payload is not
evidence that a job has been submitted or started.
Use only Amendment 3 §5.2's exact normal command with Amendment 3A's transformations and the selected single registry
suffix, as retained by 3B. No new flag, date, boundary, force option or alternate launcher is authorized. The launched process
must verify the retained equality before database access; the pre-launch control-plane inspection does not pretend to inspect
a future process:

```text
RENDER_GIT_COMMIT == ATOM_V1B_AUTHORIZED_MAIN_SHA == git rev-parse HEAD == E
```

Retain source/build/configuration and ruleset/ref observations bracketing the native submission, the returned actual job ID,
and existing control-plane evidence. Resolve uncertainty by listing/retrieving jobs, never by blind resubmission. The base
remains suspended/inert with either secret installed.
Within the one invocation, execute the unchanged startup/artifact, authenticated source/approval/history, restricted-PAT
transport, direct-host TLS/SCRAM, reader/OID, privilege/RLS and snapshot checks in their frozen order. Required GitHub
reads must succeed with the exact restricted PAT before database access. Retain the single read-only REPEATABLE READ
snapshot, causal/count-only readiness resolver, earliest qualifying boundary and all manifest prerequisites. Outside this
invocation, the operational checklist does not run a separate live readiness scan or evidence-bearing smoke test.

Operational approval does not promise readiness. HOLD, WAIT, refusal or failure ends this attempt under its exact existing
output rules; none permits automatic retry or a different manifest. Only after every prerequisite and readiness predicate
passes may the exact candidate seal be emitted and successfully captured by the existing sink, before any protected
calculation. There is no new human pause between seal and results. Keep pre-seal/final remeasurement, prior-look
exclusion and exact final authority checks unchanged.

## 11. Recovery, protective stopping, receipt publication and closure
Amendment 3B §§4-8 and the retained Amendment 2A/3/3A procedures control without replacement. In particular:

| Observed stage | Required retained disposition |
|---|---|
| New attempt, failure before evidence access | Existing BLOCKED/setup route; no fabricated process receipt. |
| Evidence read, failure before a seal | Existing null-seal PRE-CELL INVALID route; no removed capacity-only BLOCKED exception. |
| Valid seal emitted or accepted for recovery, failure before complete truthful cells | Consuming failure/recovery route; no replacement look. |
| Complete truthful cells, failed final authority check | POST-EVALUATION AUTHORITY INVALID as prescribed. |
| External failure without a truthful complete runtime receipt | Existing incident route; never invent a negative receipt from an API error. |
Preserve original E, B, canonical seal bytes, identities, dispatch/runtime binding and first-complete-receipt obligation. Each
exact recovery requires its own action-time Owner approval and verified termination/exclusion of the prior job, under the same
valid window and retained original artifact. Submit only the exact retained seal transport with original canonical bytes plus LF
and full round-trip validation. No compression, shortened lineage, new source, reserialized seal, replacement environment
variable, later boundary, new build or new seal is authorized.
Recovery transport is EXACT_SUBMISSION_OR_CONSUMING_INCIDENT, not a guarantee. Rejection, truncation, missing
acknowledgement or unavailable transport is reconciled under the retained job/status and incident rules. It neither restores a
look nor authorizes an automatic retry. Submit any first complete truthful receipt already produced. A sealed obligation without
a conforming terminal receipt remains incomplete pending the retained documentation-first incident disposition.
Retain the complete seal and first complete canonical evaluated/negative receipt, hash and required outputs before teardown
or log loss. Do not disclose partial protected values, selectively discard a result, overwrite earlier evidence or fabricate a
receipt for HOLD/WAIT/usage output that requires none. Preserve exact schemas, truthful-null fields and official filenames.
After every outcome, verify the native job itself is terminal and the base is suspended with the inert command. For a protective
stop, cancel/verify the one-off job and suspend/verify the base; parent suspension alone is insufficient. Suspected startup drift
or credential exposure retains the existing revocation/containment/incident route. Cleanup does not erase a seal or authorize
a replacement credential.
Keep the selected no-ref rule active through the exact post-terminal control/history audit and any valid recovery or required
incident disposition. Follow Amendment 3 §4's clean-closure branches exactly; missing output is not proof of no seal unless
the retained independent pre-application evidence establishes that fact. Only then may the Owner delete the exact rule and
verify lawful closure. Receipt PR preparation/review may use permitted non-ref metadata while the window is active, but no
receipt merge or other ref update occurs before lawful closure.
Publish the first complete receipt nonselectively through the existing separately reviewed, required-check-green,
Owner-merged receipt process and ChatGPT Pro final audit. This does not add a receipt file to the 3C PR. Preserve normal
mandatory PAT removal/revocation when the retained global closure conditions establish no remaining authorized invocation
or valid recovery obligation; any bounded retention serves only existing authorized recovery/closure, not a standing worker.
Protective revocation remains immediate when required by the incident rules. Any later manifest needs its own lawful window
and explicit Owner approval; this checklist does not automatically start it.

## 12. Documentation PR, effectivity and Codex handoff
The amendment PR must add exactly one Markdown file, the sole path in the header at mode 100644. No other file is
added, edited, renamed or deleted. In particular, no code, test, dependency, certificate, migration, workflow, configuration,
receipt, root freeze or phase pointer changes. This drafting/PR phase performs no probe, deployment, database mutation,
credential action, restart, seal or protected calculation.
Before merge, require a substantive independent review on the exact final head, covering the incorporated controlling
sections and the complete ordered gate contract. No same-author review, earlier-head clearance, usage-limit message,
requested review or green summary badge substitutes. The reviewer must explicitly examine the probe-before-secret
sequence; migration's LOGIN-dependent placement; unique implementation integration; actual lossless continuity;
provider-only scoped pooler acceptance; current payload-v2/window semantics; and preservation of all
recovery/receipt/security boundaries.
Record the exact candidate head, file mode/blob/raw digest, reviewer identity and final-head verdict, each material finding's
verified disposition, zero unresolved material findings/threads, and the actual required check names/results. Every required
check must be green on the applicable exact head/base; missing, pending, skipped, neutral, cancelled or failing is not green.
Do not waive or reinterpret a required check because main lacks standing protection. A changed head requires renewed
exact-head review and checks. Recheck actual head, base/history, findings and checks immediately before merge.
Only the Owner may merge. That authenticated lawful merge is M_3C. It authorizes Gate 1 inspection and preparation, not
certification of later facts. Codex may then execute only this ordered checklist, stopping at every action-time Owner approval
gate. A capability, precondition or provider-evidence failure is a stop - not permission to repair frozen implementation or
infrastructure beyond existing authority.
No protected V1B calculation begins until every applicable prerequisite actually passes and the existing seal-before-results
transition succeeds. This document does not assert that provider pooler coverage, lossless continuity, rotation/restart
completion, migration preconditions/application, credentials, probe acceptance, readiness or invocation approval already
exist.

## Source and verification anchors
Repository sources consulted for this proposal were the authenticated PR #333 metadata and changed-file listing; the main
response identifying M_impl and its parents; Amendment 3B at M_3B; and the incorporated Amendment 3, V1A, merged
scorecard constants and migration 033 at M_impl. These establish drafting anchors, not a substitute for the complete gated
audit. Amendment 3B's fixed blob is d0af73c9ff3f4f69df464a8d1bcc7ea1ba59dada; its raw SHA-256 bound by the
merged implementation is 29891d4dad2d74cd679377b3f97442b643fea97894f02124c6c4b74519d8897f.
Public Render One-Off Jobs, Render Environment Variables and Secrets, and Supabase Supavisor FAQ documentation were
consulted on 2026-09-09 for the general platform background. They are not project/reader-scoped operational acceptance
evidence, a native-layer attestation, or a command-capacity/restart-duration guarantee. Actual action-time evidence remains
mandatory.
